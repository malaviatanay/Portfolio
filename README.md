# Portfolio

Personal portfolio site, built with React + Vite.

## Develop

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Outputs static files to `dist/`, deployable to any static host (GitHub Pages, Netlify, Vercel).
